import{test,expect}from'@playwright/test';import AxeBuilder from'@axe-core/playwright';test('landing and profile have no serious axe violations and work by keyboard',async({page})=>{await page.goto('/health-and-safety-healthcheck/');for(const stage of ['landing','profile']){const results=await new AxeBuilder({page}).withTags(['wcag2a','wcag2aa']).analyze();expect(results.violations,stage).toEqual([]);if(stage==='landing'){await page.getByRole('button',{name:'Start my Healthcheck'}).focus();await page.keyboard.press('Enter');await expect(page.getByRole('heading',{name:'Your organisation'})).toBeFocused();}}});
